services:
  banter-server:
    build: .
    image: banter/server:0.1.0
    container_name: banter-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
